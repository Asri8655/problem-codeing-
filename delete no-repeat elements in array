#include <stdio.h>

int main()
{
    int a,i,j;
    scanf("%d \n",&a);
    int b[a];
    for(i=0;i<a;i++)
    {
        scanf("%d ",&b[i]);
    }
    for(i=0;i<a;i++)
    {
        for(j=0;j<a;j++)
        {
            if(i==j)
            {
                continue;
            }
            else if(b[i]==b[j])
            {
                printf("%d ",b[j]);
                break;
                
            }
        }
        
    }
}
